# DocuFlow v1.7 — Agent Prompts (Soft Deprecation / Core-vs-Advanced Split)

Hand-off prompts for Claude / Copilot coding agents. Each prompt is **self-contained** —
the agent walks in cold with no prior context from this session.

**Parent tracking issue:** [#1 — Philosophy Reset](https://github.com/shaifulshabuj/docuflow-mcp/issues/1)
**Milestone narrative:** [umbrella comment on v1.7 kickoff](https://github.com/shaifulshabuj/docuflow-mcp/issues/1#issuecomment-4489337369)
**Repo:** `shaifulshabuj/docuflow-mcp`
**Branch convention:** `v1.7/issue-<N>-<slug>`
**Bright line:** zero command removal through v2.x. v1.7 is *relabelling*, not deletion.

## Priority order

| Order | Issue | Why this slot | Parallelizable? |
|-------|-------|---------------|-----------------|
| **P0-a** | #12 — `docuflow query` core CLI | Headline value-out pipe; independent file/tool from #13 | Yes — with #13 |
| **P0-b** | #13 — `docuflow ingest` core CLI | Value-in counterpart; independent file/tool from #12 | Yes — with #12 |
| **P1** | #14 — CLI help core/advanced restructure | Depends on #12 + #13 existing so they appear under Core | No — wait for deps |
| **P2-a** | #15 — CLAUDE.md auto-gen featuring 4 core tools | Doc-only. Settles after #14's new structure | Yes — with #16 |
| **P2-b** | #16 — `MIGRATION.md` philosophy + command map | Doc-only. References #14's command mapping | Yes — with #15 |

---

## Universal preamble (paste at top of every prompt below)

```
You are working in the DocuFlow MCP repo at
/Volumes/SATECHI_WD_BLACK_2/dev/docuflow-mcp.

Rules of the room:
- This repo runs under Waymark. ALL file and shell operations MUST go through
  mcp__waymark-docuflow-mcp__read_file / write_file / bash. Do not use built-in
  file or shell tools. If Waymark blocks a path, accept the block — do not
  retry via a different tool.
- Continue autonomously until the acceptance criteria are met. Do not pause
  to ask the user for clarification — make reasonable judgment calls and proceed.
  If you hit a genuine blocker (e.g. missing dependency, contradictory spec),
  document the decision in the PR body and continue with the best alternative.
- Use the Explore sub-agent for codebase surveys before editing when the scope
  spans more than ~3 files. Use Plan sub-agent only if the task requires non-
  obvious architectural decisions. Otherwise, work directly.
- The MORATORIUM in #1 is still in force:
    ❌ No new MCP tools (the 4-core/11-advanced split keeps the count at 15)
    ❌ No new CLI subcommands beyond `query` and `ingest` (and these wrap
       EXISTING MCP tools — no new server-side capabilities)
    ❌ No new UI views, no new integrations
    ✅ Surface labelling, CLI ergonomics, doc clarity, migration narrative
- The BRIGHT LINE: zero command removal through v2.x. Every existing
  `docuflow watch`, `docuflow sync --ai`, `docuflow ui`, etc. must KEEP working
  at its old top-level path. The `advanced` prefix is OPTIONAL everywhere.
- Validation before commit:
    1. npm run build                              (must pass)
    2. npm run test -w packages/server            (must pass)
    3. bash scripts/pre-release-check.sh          (must print "RESULT: PASSED")
- Commit conventions:
    - One commit per logical change, prefixed `feat(#N):` / `docs(#N):` /
      `refactor(#N):` matching the issue type
    - Include trailer:
        `Co-authored-by: Claude Sonnet 4.6 <noreply@anthropic.com>`
      (or `Co-authored-by: GitHub Copilot <copilot@github.com>` if Copilot)
    - Reference the issue: `Closes #N` in the final commit body
- When done: push the branch, open a PR titled `[v1.7 #N] <short title>`,
  link the issue (`Closes #N`), and post a one-paragraph summary of changes.
```

---

# P0-a — Issue #12: Add `docuflow query` core CLI command

```
Issue: https://github.com/shaifulshabuj/docuflow-mcp/issues/12
Branch: v1.7/issue-12-docuflow-query
[Paste universal preamble above]

## Goal
Make `query_wiki` callable as a one-liner from the shell. Today it's only an
MCP tool — agents can call it but humans can't drive the value pipe directly.

Usage we're shipping:
    docuflow query "How does the rewiki migration work?"
    docuflow query "what does ingest_source extract" --max-sources 5
    docuflow query "..." --json
    docuflow query "..." --no-cite
    docuflow query "..." --save-as "rewiki-flow"

## Required reading
Use the Explore sub-agent with this prompt:
    "Survey how DocuFlow CLI commands invoke server-side tools. Starting from
    packages/cli/src/commands/rewiki.ts, identify the `loadServerTool()`
    pattern, how it falls back through @doquflow/server vs local dist, and
    how rewiki.ts shapes output (colour helpers, exit codes, --dry-run /
    --quiet flag handling). Also identify the query_wiki tool signature
    in packages/server/src/tools/query-wiki.ts — its input params and
    return shape. Report function names, file paths, and signatures
    under 300 words."

## Implementation

1. New file: `packages/cli/src/commands/query.ts`. Mirror rewiki.ts's structure:
   - Reuse the `loadServerTool` closure pattern
   - Reuse the colour helpers (or import from a shared place if rewiki.ts has
     factored them — check the Explore report)
   - Direct invocation of query_wiki — DO NOT add a new MCP tool

2. Argument parsing:
   - Positional: the question string (required)
   - Flags: `--max-sources <N>` (default 5), `--json`, `--no-cite`,
     `--save-as <title>`, `--quiet`
   - Mirror the index.ts hasFlag/getFlagValue pattern already in use

3. Output formats:
   - Default: human-readable markdown — render the `answer` field, then a
     blank line, then a "## Sources" section listing each source page with
     its title and ID
   - `--json`: print JSON.stringify of the raw tool response (one object,
     2-space indent)
   - `--no-cite`: print only the `answer` field

4. Save flow: when `--save-as <title>` is set, after rendering the answer,
   call `save_answer_as_page` with the title and the answer content. Print
   the new page ID on stderr so it doesn't pollute --json stdout.

5. Exit codes:
   - 0: answer produced
   - 2: fatal (no .docuflow/ in cwd, server tool missing)
   - 3: query produced no matching sources (so `query "..." || handle-empty`
     works in shell pipelines)

6. Register in `packages/cli/src/index.ts` as a top-level command:
   ```ts
   } else if (cmd === 'query') {
     const question = rest[0]; // first positional after `query`
     // ... rest of flag parsing
     import('./commands/query').then(m => m.run({ question, ... }));
   }
   ```

7. README.md: add `docuflow query` as the first row in the "Common workflows"
   section (above `docuflow init`). Use the rewiki block as the format template.

8. CLAUDE.md template in `packages/cli/src/commands/init.ts`: mention
   `docuflow query` in the workflows section as the headline CLI command.
   (Issue #15 will do a deeper restructure later — for now just add a single
   reference line so v1.7's headline command is discoverable.)

## Tests

Add `packages/cli/src/__tests__/query.test.ts` (or use the existing test
layout under packages/server if the cli package doesn't have a __tests__/ dir
yet — check during Explore). Cover:
- Missing question argument → exit code != 0, helpful error
- Missing .docuflow/ → exit code 2
- --json output is valid JSON (use JSON.parse on captured stdout)
- --no-cite output omits the "## Sources" section

## Acceptance criteria

- [ ] `docuflow query "..."` returns a useful answer in under 2 seconds on
      this repo's wiki (after `docuflow rewiki` has run to produce the
      tightened 96-entity dataset)
- [ ] `docuflow query "..." --json | jq .answer` returns the answer string
- [ ] `--save-as "<title>"` creates a wiki page that survives a subsequent
      `docuflow rewiki` (verify by content hash before/after)
- [ ] Unit tests cover all 4 cases listed above
- [ ] README updated with `docuflow query` as the headline workflow
- [ ] CLAUDE.md template updated to mention `docuflow query`
- [ ] `npm run build` passes
- [ ] `npm run test -w packages/server` passes
- [ ] `bash scripts/pre-release-check.sh` → RESULT: PASSED

## Out of scope

- Streaming output / interactive REPL (post-v1.7)
- Multi-turn conversation memory (feature, not pipe — violates moratorium)
- Embedding / vector search backend swap (would change pipe semantics)
- Touching the CLI help layout (that's issue #14)

Closes #12.
```

---

# P0-b — Issue #13: Add `docuflow ingest` core CLI command

```
Issue: https://github.com/shaifulshabuj/docuflow-mcp/issues/13
Branch: v1.7/issue-13-docuflow-ingest
[Paste universal preamble above]

## Goal
Make `ingest_source` callable as a one-liner from the shell. Same reasoning as
#12 but for the value-IN side: user drops a markdown file in .docuflow/sources/,
then runs one command to fold it into the wiki.

Usage:
    docuflow ingest auth-design.md
    docuflow ingest --all
    docuflow ingest auth-design.md --dry-run
    docuflow ingest --all --quiet

## Required reading
Use the Explore sub-agent with this prompt:
    "Survey two things in the DocuFlow repo:
     (1) packages/cli/src/commands/rewiki.ts — the `loadServerTool` closure
         pattern, colour helpers, exit code conventions, --dry-run handling.
     (2) packages/server/src/tools/ingest-source.ts — signature of the
         ingest_source tool: input params (project_path, source_filename),
         return shape (pages_created, entities_discovered, etc.).
     Report function names, signatures, and the dry-run hook point if any.
     Under 250 words."

## Implementation

1. New file: `packages/cli/src/commands/ingest.ts`. Mirror rewiki.ts.

2. Arg parsing:
   - Positional: source filename (relative to .docuflow/sources/) — required
     unless `--all` is set
   - Flags: `--all`, `--dry-run`, `--quiet`

3. Behaviour:
   - Default: read one file from .docuflow/sources/ and call ingest_source.
     Print a summary: source_id, pages_created, entities_discovered.
   - `--all`: list every file in .docuflow/sources/, call ingest_source for
     each sequentially. Print a one-line summary per file, then an aggregate
     "Total: X pages from Y sources" at the end.
   - `--dry-run`: invoke the tool with a dry-run flag if it supports one
     (check during Explore). If it doesn't support dry-run natively, ALSO
     do not implement a parallel simulator — instead print "dry-run not
     supported for ingest_source; use `docuflow rewiki --dry-run` for a
     full simulation" and exit 0. Document this in the PR if you go that
     route.
   - `--quiet`: suppress per-file logs, only print the aggregate

4. Exit codes:
   - 0: success
   - 2: fatal (no .docuflow/ in cwd, server tool missing)
   - 3: source file not found (so CI scripts can detect typos)

5. Register in index.ts as a top-level command (mirror the query registration
   pattern from #12 if that's already merged; if not, use the existing
   command registration style from the surrounding code).

6. README.md: add `docuflow ingest` to the "Common workflows" section
   immediately after `docuflow query`.

7. CLAUDE.md template in init.ts: mention `docuflow ingest` next to
   `docuflow query` in the workflows section.

## Tests

`packages/cli/src/__tests__/ingest.test.ts` (or wherever query.test.ts lives
if #12 merged first — match the structure). Cover:
- Missing source file → exit code 3
- Missing .docuflow/ → exit code 2
- `--all` with empty sources/ → exit code 0, "0 sources" message (no crash)
- `--all` with multiple files → aggregates correctly

## Acceptance criteria

- [ ] `docuflow ingest architecture_design.md` matches the per-file page count
      that `docuflow rewiki` reports for that source on this repo
- [ ] `docuflow ingest --all` is idempotent (run twice → same wiki state,
      no duplicates introduced)
- [ ] `--dry-run` either truly previews without writes, OR clearly redirects
      to rewiki --dry-run with a one-line message (documented in PR body)
- [ ] Unit tests cover all 4 cases listed above
- [ ] README updated
- [ ] CLAUDE.md template updated
- [ ] `npm run build` passes
- [ ] `npm run test -w packages/server` passes
- [ ] `bash scripts/pre-release-check.sh` → RESULT: PASSED

## Out of scope

- Concurrent multi-file ingestion (sequential is fine for the core surface)
- Source file watchers (`docuflow watch` already does that — advanced surface)
- Custom extraction rules per-source (rules are global in extractor-rules.ts)
- Touching the CLI help layout (that's #14)

Closes #13.
```

---

# P1 — Issue #14: Restructure CLI help (core vs advanced)

```
Issue: https://github.com/shaifulshabuj/docuflow-mcp/issues/14
Branch: v1.7/issue-14-help-restructure
[Paste universal preamble above]

## DEPENDENCY
This issue depends on #12 (docuflow query) AND #13 (docuflow ingest) being
merged first. Verify both are on main before starting:
    git fetch origin main
    git log --oneline origin/main | grep -E "(#12|#13)"
Both must appear. If either is missing, STOP and report.

## Goal
Reorganise `docuflow --help` so the irreducible CORE is visible first and
operational scaffolding is one level deeper. ZERO command removal: every
existing top-level path must keep working unchanged.

After this PR ships:
    docuflow --help              shows 5 core commands + "see advanced --help"
    docuflow advanced --help     shows 9 advanced commands
    docuflow <any-old-cmd>       still works exactly as today
    docuflow advanced <cmd>      also works (new optional path)

## Required reading
Use the Explore sub-agent with this prompt:
    "Map the complete CLI command surface in packages/cli/src/index.ts.
    For each `cmd ===` branch, report: command name, what command file it
    imports from, what flags it parses. Also identify how --help is
    currently rendered (inline strings? separate file? help command?).
    Group commands by which would belong in CORE (init, status, query,
    ingest, rewiki) vs ADVANCED (watch, sync, ui, start, lint, review,
    recent, suggest, update). Under 400 words."

## Implementation

1. New file: `packages/cli/src/commands/help.ts`. Export:
   - `printCoreHelp()` — the new top-level help
   - `printAdvancedHelp()` — the advanced subset
   The existing inline help text in index.ts moves here. Use template literals
   for readability; no external help library.

2. Update `packages/cli/src/index.ts`:
   - When `cmd === '--help' || cmd === '-h' || cmd === undefined`:
     call `printCoreHelp()`
   - When `cmd === 'advanced'`:
     peel off `rest[0]` as the new command and re-dispatch:
       ```ts
       if (cmd === 'advanced') {
         const [advCmd, ...advRest] = rest;
         if (!advCmd || advCmd === '--help' || advCmd === '-h') {
           return printAdvancedHelp();
         }
         // re-route: substitute advCmd for cmd and recursively dispatch
         return dispatch(advCmd, advRest);
       }
       ```
     The cleanest implementation: extract the existing if/else-if chain into
     a `dispatch(cmd, rest)` function and call it twice — once for the
     bare invocation, once from the `advanced` re-route.

3. Help text format (match exactly — these strings ship to users):

   `docuflow --help`:
   ```
   DocuFlow — preserve decision context for AI agents.

   CORE
     docuflow init                         Initialise .docuflow/ in this project
     docuflow ingest <file>                Add a source document to the wiki
     docuflow query "<question>"           Ask the wiki — answer with citations
     docuflow status                       Show wiki health and counts
     docuflow rewiki                       Migrate / re-ingest with current rules

   ADVANCED
     docuflow advanced --help              See watch / sync / ui / lint / review / recent / suggest / start / update

     docuflow --version                    Print version
     docuflow --help                       Show this help
   ```

   `docuflow advanced --help`:
   ```
   DocuFlow — advanced surface (sync daemons, UI, audit commands).

     docuflow advanced watch [stop|status|restart]    Auto-sync daemon
     docuflow advanced sync [--ai]                    One-shot sync for CI / git hooks
     docuflow advanced ui [--port N]                  Launch web UI dashboard
     docuflow advanced start                          Alias for ui
     docuflow advanced lint                           Wiki health audit
     docuflow advanced review                         Review uncommitted changes
     docuflow advanced recent                         Recent work dashboard
     docuflow advanced suggest                        First-steps guidance
     docuflow advanced update                         Self-upgrade DocuFlow

   Note: every "advanced" command also works at its old top-level path
   (e.g. `docuflow watch` is still valid). The "advanced" prefix is optional.
   ```

4. NO deprecation warnings. The bare-form commands print nothing extra —
   they just work. (Deprecation warnings land in v2.0 with the package split,
   per umbrella #1.)

5. README.md: replace the current help/usage block with the new two-tier
   layout. Add a note "Existing scripts using `docuflow watch`, `docuflow sync`,
   etc. continue to work unchanged."

6. CLAUDE.md template in init.ts: regenerate the workflows section to
   reflect the new grouping (init/ingest/query/status/rewiki as Core).

## Tests

`packages/cli/src/__tests__/help.test.ts`:
- `docuflow --help` output contains the 5 core command names AND the string
  "ADVANCED"
- `docuflow --help` output does NOT contain `watch`, `sync`, `ui`, `lint`,
  `review`, `recent`, `suggest`, `update` (those are advanced only)
- `docuflow advanced --help` output contains all 9 advanced commands
- `docuflow advanced --help` output does NOT contain `init`, `ingest`, `query`
- `docuflow advanced watch stop` exits the same as `docuflow watch stop`
  (mock the watch module; assert it's called once with `stop` argv)
- `docuflow advanced sync --ai` invokes the sync module with the same options
  as `docuflow sync --ai`
- `docuflow query "test"` still works (bare core command unchanged)
- `docuflow watch` (bare form) still works — print a status check rather than
  actually running the daemon (or mock it)

## Acceptance criteria

- [ ] `docuflow --help` shows the new core/advanced layout, 5 core commands
- [ ] `docuflow advanced --help` shows 9 advanced commands
- [ ] Every old top-level command still works:
      watch, sync, ui, start, lint, review, recent, suggest, update
- [ ] Every `docuflow advanced <cmd>` invocation works identically to the
      bare form (test 3 representative ones: watch, sync --ai, ui)
- [ ] Unit tests cover both surfaces and the bare-vs-advanced equivalence
- [ ] README updated; old help screenshot/example replaced
- [ ] CLAUDE.md template reflects the new grouping
- [ ] `npm run build` passes
- [ ] `npm run test -w packages/server` passes
- [ ] `bash scripts/pre-release-check.sh` → RESULT: PASSED

## Out of scope

- Deprecation warnings on bare-form advanced commands (v2.0)
- Removing ANY command (v3.0 at earliest, evidence-based)
- Changing command behaviour — only help text and routing change here
- Touching the MCP tool surface (that's #15)

Closes #14.
```

---

# P2-a — Issue #15: CLAUDE.md auto-generation update

```
Issue: https://github.com/shaifulshabuj/docuflow-mcp/issues/15
Branch: v1.7/issue-15-claude-md-core-tools
[Paste universal preamble above]

## DEPENDENCY (soft)
Best landed after #14 ships so the CLI grouping and the tool grouping match.
If #14 isn't merged yet, you can still do this — just align with the
core/advanced split described in #14's prompt.

## Goal
The `docuflow init` command auto-generates a CLAUDE.md so AI agents
auto-discover DocuFlow on session start. Today that file lists all 15 MCP
tools as a flat menu. After this PR, the 4 CORE tools are featured and the
11 ADVANCED tools are demoted to a second section.

The MCP server still registers all 15 tools — this is doc reorganisation only.

## Required reading
Use the Explore sub-agent with this prompt:
    "Find the CLAUDE.md template generator in packages/cli/src/commands/init.ts.
    Report: (1) what string/template the file uses for the DocuFlow section,
    (2) whether it uses markers like <!-- BEGIN DOCUFLOW --> / <!-- END
    DOCUFLOW --> for idempotent re-runs, (3) how it handles existing
    CLAUDE.md files (overwrite? merge? insert?). Under 200 words."

## The 4 core MCP tools (feature these)

1. `query_wiki({ project_path, question })` — ask the wiki, returns answer + citations
2. `ingest_source({ project_path, source_filename })` — fold a source into the wiki
3. `wiki_search({ project_path, query })` — BM25 search across pages
4. `read_module({ path })` — read + extract facts from a single source file

## The 11 advanced MCP tools (demote)

`list_modules`, `list_wiki`, `write_spec`, `read_specs`, `save_answer_as_page`,
`synthesize_answer`, `update_index`, `lint_wiki`, `get_schema_guidance`,
`preview_generation`, `generate_dependency_graph`

## New CLAUDE.md DocuFlow section (template)

```markdown
# DocuFlow — AI Documentation Assistant

DocuFlow preserves decision context for AI agents. Intent in, value out.

## Core tools (use these first)

- **query_wiki({ project_path, question })** — Ask the wiki. Returns an answer with citations.
- **ingest_source({ project_path, source_filename })** — Fold a markdown source into the wiki.
- **wiki_search({ project_path, query })** — BM25 search across all pages.
- **read_module({ path })** — Read and extract facts from a single source file.

## Workflows

### Answer a question
\`\`\`
query_wiki({ project_path: ".", question: "How does authentication work?" })
\`\`\`

### Add new context
\`\`\`
# user drops a markdown file in .docuflow/sources/
ingest_source({ project_path: ".", source_filename: "auth-design.md" })
\`\`\`

## Advanced tools

Use when the core tools don't cover the workflow. Each has more parameters
and side effects.

- **list_modules** — Walk a directory tree and extract facts in bulk
- **list_wiki** — Inventory pages by category, with staleness flags
- **write_spec / read_specs** — Persistent agent-written specs
- **save_answer_as_page** — Promote a synthesised answer into the wiki
- **synthesize_answer** — Combine multiple pages into a markdown synthesis
- **update_index** — Rebuild `.docuflow/index.md`
- **lint_wiki** — Health checks: orphans, broken refs, stale content
- **get_schema_guidance** — Recommend what pages should exist
- **preview_generation** — Show what a tool will do before running
- **generate_dependency_graph** — Build the import/shared-table graph
```

## Implementation

1. Edit `packages/cli/src/commands/init.ts` — locate the DocuFlow section
   template (identified during Explore)
2. Replace the existing flat tool list with the two-section format above
3. Idempotency: if the file uses `<!-- BEGIN DOCUFLOW --> ... <!-- END DOCUFLOW -->`
   markers, the regenerator must replace ONLY the content between markers.
   If no markers exist yet, ADD them so future re-runs are idempotent.
4. Also update THIS repo's `CLAUDE.md` directly (manual edit) to mirror the
   new structure — the dogfood must demonstrate the pattern.

## Tests

`packages/cli/src/__tests__/init-claude-md.test.ts`:
- Fresh init writes the new two-section structure
- Re-running init on an existing CLAUDE.md is idempotent (string equality
  between two consecutive generations)
- Hand-edited content OUTSIDE the markers is preserved across re-runs
- The output contains exactly 4 core tools in the Core section
- The output contains exactly 11 advanced tools in the Advanced section

## Acceptance criteria

- [ ] `docuflow init` in a fresh project produces CLAUDE.md with the new
      two-tier layout
- [ ] Running `docuflow init` again is idempotent (`diff` produces no output)
- [ ] This repo's own CLAUDE.md updated to match (manual but tracked in PR)
- [ ] The 4 core tools appear first; the 11 advanced tools after a separator
- [ ] Workflows section references ONLY `query_wiki` and `ingest_source`
- [ ] Idempotency markers (`<!-- BEGIN/END DOCUFLOW -->`) present in generated
      output
- [ ] Unit tests cover the 5 cases listed above
- [ ] `npm run build` passes
- [ ] `npm run test -w packages/server` passes
- [ ] `bash scripts/pre-release-check.sh` → RESULT: PASSED

## Out of scope

- Removing tools from the server registration (those tools must keep working
  for every existing agent that uses them — bright line)
- AGENTS.md / opencode.json restructure (single source of truth is CLAUDE.md)
- New tools, new workflows beyond the 2 documented

Closes #15.
```

---

# P2-b — Issue #16: Add `MIGRATION.md`

```
Issue: https://github.com/shaifulshabuj/docuflow-mcp/issues/16
Branch: v1.7/issue-16-migration-md
[Paste universal preamble above]

## DEPENDENCY (soft)
Best landed alongside or after #14 (so the command mapping table reflects
the final new help structure). You can draft in parallel — just keep the
mapping table easy to update.

## Goal
Write a `MIGRATION.md` at repo root that:
- Explains the philosophy reset in the project's own voice
- Quotes the LinkedIn post on the Tracker's Paradox
- Maps every existing command to its new preferred path
- States explicitly: "every old command still works through v2.x"
- Looks ahead to v2.0 (package split) and v3.0 (evidence-based pruning)

This is a documentation task. No code changes.

## Required reading (no sub-agent needed)

Read these files first to lift the right tone and structure:
- `release/v1.6.0.md` — the philosophy-reset narrative shipped in v1.6
- Umbrella issue #1 body — has the staged plan and the moratorium list
- The LinkedIn post quoted at the top of #1: https://www.linkedin.com/posts/shaifulshabuj_softwareengineering-productdevelopment-softwarearchitecture-ugcPost-7461730048954474496-ASZF
- (If #14 has merged) the new help screens in `packages/cli/src/commands/help.ts`

## File structure (≤200 lines total)

```markdown
# Migrating to DocuFlow v1.7

> "Every tool built to manage work eventually becomes work itself."
> — [LinkedIn post on the Tracker's Paradox](<URL>)

## Why this is happening

[ONE paragraph: DocuFlow had drifted into the very anti-pattern the post warns
about. v1.5.2 had 11 CLI subcommands, 15 MCP tools, a watch daemon, an AI-
bridge selector, a UI, a REST API. The value-out promise was buried under
operational scaffolding. The philosophy reset returns DocuFlow to its core:
intent in, value out, nothing in between.]

[ONE paragraph: The test every PR now answers in one sentence — "Does this
make the user's problem easier, not our system easier to build?"]

## What changed in v1.6 (quality pass)

[Brief: tightened entity extractor, 256 → 96 pages on the dogfood, 76 tests.
Zero surface changes — every command still works.]

## What changes in v1.7 (this milestone)

- New core CLI commands: `docuflow query "<question>"` and `docuflow ingest <file>`
- Help screen reorganised: CORE (5 commands) at the top, ADVANCED (9 commands)
  one layer deeper
- **Every existing command still works at its old path.** The `advanced`
  prefix is optional everywhere.

## Command mapping

| Old path | New preferred path | Still works as-is? |
|---|---|---|
| `docuflow init`     | `docuflow init`               | yes (core) |
| `docuflow status`   | `docuflow status`             | yes (core) |
| `docuflow rewiki`   | `docuflow rewiki`             | yes (core) |
| `docuflow watch`    | `docuflow advanced watch`     | yes, indefinitely |
| `docuflow sync`     | `docuflow advanced sync`      | yes |
| `docuflow ui`       | `docuflow advanced ui`        | yes |
| `docuflow start`    | `docuflow advanced ui` (alias)| yes |
| `docuflow lint`     | `docuflow advanced lint`      | yes |
| `docuflow review`   | `docuflow advanced review`    | yes |
| `docuflow recent`   | `docuflow advanced recent`    | yes |
| `docuflow suggest`  | `docuflow advanced suggest`   | yes |
| `docuflow update`   | `docuflow advanced update`    | yes |
| (new)               | `docuflow query "..."`        | new in v1.7 |
| (new)               | `docuflow ingest <file>`      | new in v1.7 |

## CLAUDE.md auto-generation

`docuflow init` now writes a CLAUDE.md that features the 4 CORE MCP tools
(`query_wiki`, `ingest_source`, `wiki_search`, `read_module`) and demotes
the other 11 to an "Advanced" section. The server still registers all 15 —
the change is doc-only. Existing agents using `lint_wiki`, `save_answer_as_page`,
etc. continue to work unchanged.

## What's coming in v2.0 (package split)

[Brief: `@doquflow/core` (irreducible MCP server) and `@doquflow/studio`
(UI/REST/daemons). `@doquflow/cli` stays as a meta-package depending on
both → existing install path unchanged.]

## What's coming in v3.0 (evidence-based pruning)

[Brief: if studio features show real usage, they earn their keep. If not,
they retire. Decision deferred until there's evidence.]

## The "no" list until v2.0

Every PR has to answer the question above in one sentence. Until v2.0:

- ❌ No new MCP tools
- ❌ No new CLI subcommands
- ❌ No new UI views
- ❌ No new integrations
- ✅ Extractor precision, schema authoring UX, query-result quality, value-out docs

## Questions?

Open an issue on the repo. The umbrella tracking this whole reset is #1.
```

## Implementation steps

1. Write the file at `/MIGRATION.md` (repo root)
2. Resolve the LinkedIn URL placeholder — pull it from umbrella #1 or
   release/v1.6.0.md
3. Update the command mapping table to reflect whatever the final #14 help
   structure shipped (if #14 isn't merged yet, draft against the spec in
   #14's issue body)
4. README.md: add a one-line link at the top of the file (under the title):
   "Migrating from v1.5? See [MIGRATION.md](MIGRATION.md)."
5. release/v1.7.0.md (when it's eventually drafted by the release manager)
   should reference MIGRATION.md — make a note in the PR body that this
   link should be added then

## Acceptance criteria

- [ ] `MIGRATION.md` exists at repo root, ≤ 200 lines
- [ ] Quotes the LinkedIn post with a working link
- [ ] Has a command mapping table with at least 14 rows
- [ ] Maps every existing CLI command to its new preferred path
- [ ] States explicitly: "every old command still works through v2.x"
- [ ] Has a "no list" until v2.0 section copied from umbrella #1
- [ ] README.md links to MIGRATION.md from the top of the file
- [ ] `bash scripts/pre-release-check.sh` → RESULT: PASSED

## Out of scope

- Documenting v2.0 / v3.0 in detail (they're vague on purpose —
  evidence-based)
- Marketing-style copy / blog post tone
- Step-by-step tutorials (those go in `docs/`)
- Translating to other languages (English-only for v1.7)

Closes #16.
```

---

## Parallelization plan

Three waves. Each wave can run two agents simultaneously where indicated.

| Wave | Agent A | Agent B | Why parallel |
|------|---------|---------|--------------|
| 1 | #12 (query) | #13 (ingest) | Different files (`query.ts` vs `ingest.ts`), different server tools, only `index.ts` overlaps (different `else if` branches) |
| 2 | #14 (help restructure) | — | Must wait for #12 + #13 on main; touches `index.ts` heavily |
| 3 | #15 (CLAUDE.md auto-gen) | #16 (MIGRATION.md) | Both doc-only, different files, no conflict |

If only one agent is available, follow the priority order top-to-bottom.

## After v1.7

Once all five PRs land, run the release ritual:

```bash
docuflow query "what changed in v1.7"          # demo the new core surface
docuflow ingest --all                          # demo the value-in pipe
docuflow advanced --help                       # confirm advanced surface
docuflow watch status                          # confirm bare-form still works
git log --oneline -10                          # signal restored
```

Then bump versions to v1.7.0, mirror CHANGELOG.md ↔ release/CHANGELOG.md,
write release/v1.7.0.md (use release/v1.6.0.md as the template), tag,
push main + tag, create the GitHub release, and update umbrella #1 with
the v1.7 ship comment.

**Next milestone:** v2.0 — package split into `@doquflow/core` and
`@doquflow/studio`. Sub-issues to be filed when v1.7 lands.
