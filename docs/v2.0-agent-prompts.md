# DocuFlow v2.0 — Agent Prompts (Package Split)

Hand-off prompts for Claude / Copilot coding agents. Each prompt is **self-contained** —
the agent walks in cold with no prior context from this session.

**Parent tracking issue:** [#1 — Philosophy Reset](https://github.com/shaifulshabuj/docuflow-mcp/issues/1)
**Milestone announcement:** [v2.0 kickoff comment on #1](https://github.com/shaifulshabuj/docuflow-mcp/issues/1#issuecomment-4492522083)
**Repo:** `shaifulshabuj/docuflow-mcp`
**Branch convention:** `v2.0/issue-<N>-<slug>`
**Bright line:** zero breakage for existing users — every install command, every CLI invocation, every `.mcp.json` registration must keep working without user action.

## Priority order

| Order | Issue | Why this slot | Parallelizable? |
|-------|-------|---------------|-----------------|
| **P0** | #22 — Extract `@doquflow/core` | Foundation; everything else depends on this | No — solo wave |
| **P1** | #23 — Extract `@doquflow/studio` | Depends on #22; blocks #24 + #25 | No — solo wave |
| **P2-a** | #24 — `@doquflow/server` thin alias | Depends on #22 + #23 | Yes — with #25 |
| **P2-b** | #25 — `@doquflow/cli` meta-package | Depends on #22 + #23 | Yes — with #24 |
| **P3-a** | #26 — `docuflow doctor` diagnostic | Depends on #25 (needs new install paths to detect) | Yes — with #27 |
| **P3-b** | #27 — MIGRATION.md + release/v2.0.0.md | Depends on all PRs landed (to reference real numbers + outputs) | Yes — with #26 |

---

## Universal preamble (paste at top of every prompt below)

```
You are working in the DocuFlow MCP repo at
/Volumes/SATECHI_WD_BLACK_2/dev/docuflow-mcp.

Rules of the room:
- This repo runs under Waymark. ALL file and shell operations MUST go through
  mcp__waymark-docuflow-mcp__read_file / write_file / bash. Do not use built-in
  file or shell tools. If Waymark blocks a path, accept the block — do not
  retry via a different tool.
- Continue autonomously until the acceptance criteria are met. Do not pause
  to ask the user for clarification — make reasonable judgment calls and
  proceed. If you hit a genuine blocker, document the decision in the PR body
  and continue with the best alternative.
- Use the Explore sub-agent for codebase surveys before editing when the scope
  spans more than ~5 files. Use Plan sub-agent only if the task requires
  non-obvious architectural decisions. Otherwise, work directly.

The BRIGHT LINE is the v2.0 promise: ZERO BREAKAGE FOR EXISTING USERS.
- `npm i -g @doquflow/cli` keeps providing the full surface
- `@doquflow/server` keeps installing (becomes a thin alias of studio)
- Every existing `.mcp.json` registration works without modification
- Every CLI command works at its old path (the v1.7 advanced prefix is optional)
- The new install paths (@doquflow/core, @doquflow/studio) are ADDITIONS, not
  replacements

The MORATORIUM is still in force (slightly amended for v2.0):
  ❌ No new MCP tools (15 stays at 15 — 4 in core, 11 in studio)
  ❌ No new UI views
  ❌ No new integrations
  ⚠️ ONE new CLI subcommand permitted: `docuflow doctor` (#26 only)
     — because it's a diagnostic that helps users navigate the split,
       not a feature that gives the system another thing to do.

The package versions for this milestone: every package bumps to `2.0.0`
during the v2.0 work. Inter-package deps (e.g. studio depending on core)
use the exact version `"2.0.0"` (not a caret range — workspace consistency
matters during a coordinated split).

Validation before commit:
  1. npm run build                                    (must pass at root)
  2. npm run test -w packages/<your-package>          (must pass)
  3. bash scripts/pre-release-check.sh                (must print RESULT: PASSED)

Commit conventions:
  - One commit per logical change, prefixed `feat(#N):` / `refactor(#N):` /
    `chore(#N):` / `docs(#N):` matching the issue type
  - Include trailer:
      Co-authored-by: Claude Sonnet 4.6 <noreply@anthropic.com>
    (or `Co-authored-by: GitHub Copilot <copilot@github.com>` if Copilot)
  - Reference the issue: `Closes #N` in the final commit body

When done: push the branch, open a PR titled `[v2.0 #N] <short title>`,
link the issue (`Closes #N`), and post a one-paragraph summary of changes.
```

---

# P0 — Issue #22: Extract `@doquflow/core`

```
Issue: https://github.com/shaifulshabuj/docuflow-mcp/issues/22
Branch: v2.0/issue-22-extract-core
[Paste universal preamble above]

## Goal
Create a new package `@doquflow/core` at `packages/core/`. It contains the
4 core MCP tools, shared types, the filesystem helpers, and the extractor
rules. It is the irreducible value pipe that everything else builds on.

## Required reading
Use the Explore sub-agent with this prompt:
    "Survey the @doquflow/server package at packages/server/. Report:
     (1) all files in src/, grouped as 'core' (will move to @doquflow/core)
         vs 'advanced' (will move to @doquflow/studio). Core = the 4 tools
         ingest-source / query-wiki / wiki-search / read-module, plus types,
         filesystem, language-map, extractor, extractor-rules, extractor-stoplist,
         category-dir, and tests for those files.
     (2) For each core file, list which other server files it imports from.
         This is the dependency graph we need to preserve.
     (3) Identify any test files under packages/server/src/__tests__/ that
         cover the core tools — those move with their subjects.
     Under 400 words."

## Implementation steps

1. Create `packages/core/` directory structure:
   ```
   packages/core/
   ├── package.json
   ├── tsconfig.json
   ├── README.md          (1-paragraph: what this package is, see release/v2.0.0.md for context)
   └── src/
       ├── index.ts       (MCP server bootstrap — see step 4)
       ├── lib.ts         (library exports for @doquflow/studio to consume)
       ├── (moved files — see file-move map below)
       └── __tests__/
   ```

2. Move source files from `packages/server/src/` → `packages/core/src/`
   using `git mv` so history is preserved. **File-move map:**

   | Old path | New path |
   |---|---|
   | `packages/server/src/types.ts` | `packages/core/src/types.ts` |
   | `packages/server/src/filesystem.ts` | `packages/core/src/filesystem.ts` |
   | `packages/server/src/language-map.ts` | `packages/core/src/language-map.ts` |
   | `packages/server/src/extractor.ts` | `packages/core/src/extractor.ts` |
   | `packages/server/src/extractor-rules.ts` | `packages/core/src/extractor-rules.ts` |
   | `packages/server/src/extractor-stoplist.ts` | `packages/core/src/extractor-stoplist.ts` |
   | `packages/server/src/category-dir.ts` | `packages/core/src/category-dir.ts` |
   | `packages/server/src/tools/ingest-source.ts` | `packages/core/src/tools/ingest-source.ts` |
   | `packages/server/src/tools/query-wiki.ts` | `packages/core/src/tools/query-wiki.ts` |
   | `packages/server/src/tools/wiki-search.ts` | `packages/core/src/tools/wiki-search.ts` |
   | `packages/server/src/tools/read-module.ts` | `packages/core/src/tools/read-module.ts` |
   | `packages/server/src/__tests__/extractor-rules.test.ts` | `packages/core/src/__tests__/extractor-rules.test.ts` |
   | (Any other tests covering the 4 core tools — identify during Explore) | matching path under packages/core/ |

   The advanced tool files (list-modules, list-wiki, write-spec, read-specs,
   save-answer-as-page, answer-synthesis, update-index, lint-wiki,
   get-schema-guidance, preview-generation, generate-dependency-graph,
   build-graph) STAY in packages/server/src/tools/ for now — those move
   in #23. Do not touch them.

3. Write `packages/core/package.json`:
   ```json
   {
     "name": "@doquflow/core",
     "version": "2.0.0",
     "description": "DocuFlow core — 4 MCP tools and the persistence substrate",
     "author": "Docuflow <hello@doquflows.dev>",
     "license": "MIT",
     "homepage": "https://github.com/doquflows/docuflow",
     "repository": {
       "type": "git",
       "url": "https://github.com/doquflows/docuflow.git"
     },
     "main": "./dist/lib.js",
     "types": "./dist/lib.d.ts",
     "bin": { "docuflow-core": "./dist/index.js" },
     "files": ["dist/**", "README.md"],
     "dependencies": {
       "@modelcontextprotocol/sdk": "^1.0.4"
     },
     "devDependencies": {
       "@types/node": "^22.0.0",
       "typescript": "^5.6.0",
       "vitest": "^4.1.6"
     },
     "scripts": {
       "build": "tsc",
       "test": "vitest run"
     }
   }
   ```

4. Write `packages/core/src/index.ts` — MCP server bootstrap:
   - Imports the 4 tool handlers from `./tools/`
   - Imports the MCP SDK
   - Registers exactly 4 tools: ingest_source, query_wiki, wiki_search, read_module
   - Use the existing `packages/server/src/index.ts` as a template — strip out
     the 11 advanced tool registrations, keep only the 4 core registrations
     and their tool schemas

5. Write `packages/core/src/lib.ts` — library exports for studio:
   ```ts
   export { ingestSource } from "./tools/ingest-source";
   export { queryWiki } from "./tools/query-wiki";
   export { wikiSearch } from "./tools/wiki-search";
   export { readModule } from "./tools/read-module";
   export * as types from "./types";
   export * as filesystem from "./filesystem";
   export * as languageMap from "./language-map";
   export * as extractor from "./extractor";
   export * as extractorRules from "./extractor-rules";
   export * as categoryDir from "./category-dir";
   ```
   (Match the actual export shapes after moving — adjust based on what each
   moved file exports.)

6. Write `packages/core/tsconfig.json`:
   ```json
   {
     "compilerOptions": {
       "target": "ES2020",
       "module": "commonjs",
       "moduleResolution": "node",
       "lib": ["ES2020"],
       "strict": true,
       "esModuleInterop": true,
       "skipLibCheck": true,
       "outDir": "./dist",
       "rootDir": "./src",
       "declaration": true,
       "sourceMap": true
     },
     "include": ["src/**/*"],
     "exclude": ["src/__tests__/**/*", "dist/**/*"]
   }
   ```

7. Update root `package.json` workspaces array:
   - Before: `"workspaces": ["packages/*"]`
   - After: keep the wildcard (it auto-picks up `packages/core`); just verify

8. Update internal imports IN packages/server (if any server file imports
   from a now-moved file) to import from `@doquflow/core/lib` instead. But
   note: this issue does NOT delete the original files in packages/server.
   They stay until #24 turns server into a shim. So the temporary state is
   that BOTH packages/core AND packages/server contain the 4 core tools
   side-by-side. Existing builds continue to work; #24 deletes the
   duplicates. (Document this clearly in PR body as "zero-downtime split".)

   Wait — re-reading: this creates a duplication that the build tools may
   not handle well. Cleaner approach: in this PR, REMOVE the moved files
   from packages/server, and update packages/server's index.ts to import
   the 4 core tools FROM @doquflow/core. That way packages/server still
   registers all 15 (4 imported from core + 11 still local). Then #23
   moves the 11 to studio, and #24 makes server a 1-line shim.

   Implement the cleaner approach: move files (don't copy), update
   packages/server/src/index.ts to import the 4 core tools from
   `@doquflow/core/lib`, keep registering all 15 tools as it does today.

## Boundary rules to enforce

- `@doquflow/core` has ZERO dependencies on packages/server, packages/cli, packages/api, packages/ui
- Only runtime dep is `@modelcontextprotocol/sdk`
- No HTTP server, no daemons, no UI, no git hooks

## Acceptance criteria

- [ ] `npm install` at repo root succeeds (workspaces resolve)
- [ ] `npm run build -w packages/core` produces `dist/lib.js` AND `dist/index.js`
- [ ] `node packages/core/dist/index.js` starts an MCP server that lists EXACTLY 4 tools (use `echo '{"jsonrpc":"2.0","id":1,"method":"tools/list"}' | node packages/core/dist/index.js` to verify, or write a smoke script)
- [ ] All extractor unit tests pass under `packages/core/src/__tests__/`
- [ ] `grep -rE "@doquflow/server|packages/server|packages/cli|packages/api|packages/ui" packages/core/src` returns NOTHING
- [ ] `packages/server` still builds (it now imports core tools from `@doquflow/core/lib`)
- [ ] `packages/server`'s MCP server still registers all 15 tools (4 imported from core + 11 still local)
- [ ] `npm run test -w packages/server` still passes (no advanced tool tests broken)
- [ ] `bash scripts/pre-release-check.sh` PASSED
- [ ] PR body documents the file-move map and explains the temporary state

## Out of scope
- Studio extraction (#23)
- @doquflow/server becoming a shim (#24)
- CLI dep switch (#25)
- doctor command (#26)
- Migration docs (#27)

Closes #22.
```

---

# P1 — Issue #23: Extract `@doquflow/studio`

```
Issue: https://github.com/shaifulshabuj/docuflow-mcp/issues/23
Branch: v2.0/issue-23-extract-studio
[Paste universal preamble above]

## DEPENDENCY
This issue depends on #22 being on main. Verify before starting:
    git fetch origin main
    git log --oneline origin/main | grep -E "#22"
If not present, STOP and report.

## Goal
Create `@doquflow/studio` at `packages/studio/`. It contains the 11 advanced
MCP tools, the React web UI, the Express REST API, and supporting code.
Studio depends on @doquflow/core for the 4 fundamental tools — its MCP
server registers all 15 by composing core + advanced.

## Required reading
Use the Explore sub-agent with this prompt:
    "Survey three things in the DocuFlow repo:
     (1) packages/server/src/tools/ — list the 11 ADVANCED tool files (every
         *.ts file EXCEPT ingest-source, query-wiki, wiki-search, read-module
         which moved to core in #22). Also list any helper files like
         build-graph.ts that go with these tools.
     (2) packages/api/src/ — the REST API. Report the entry point, route
         structure, what it imports from packages/server.
     (3) packages/ui/src/ — the React UI. Report the entry point, what tools
         the UI calls (likely via the REST API), and the build configuration
         (vite.config.ts, tsconfig.json).
     Identify any cross-package imports the new packages/studio/ will need
     to wire up. Under 500 words."

## Implementation steps

1. Create `packages/studio/`:
   ```
   packages/studio/
   ├── package.json
   ├── tsconfig.json
   ├── README.md
   ├── vite.config.ts          (moved from packages/ui/)
   ├── src/
   │   ├── mcp/
   │   │   └── index.ts        MCP server registering 4 (from core) + 11 (local)
   │   ├── tools/              the 11 advanced tools moved here
   │   ├── api/                moved from packages/api/src/
   │   ├── ui/                 moved from packages/ui/src/
   │   ├── lib.ts              library exports
   │   └── __tests__/          advanced tool tests moved here
   ```

2. File-move map (use `git mv`):

   | Old path | New path |
   |---|---|
   | `packages/server/src/tools/list-modules.ts` | `packages/studio/src/tools/list-modules.ts` |
   | `packages/server/src/tools/list-wiki.ts` | `packages/studio/src/tools/list-wiki.ts` |
   | `packages/server/src/tools/write-spec.ts` | `packages/studio/src/tools/write-spec.ts` |
   | `packages/server/src/tools/read-specs.ts` | `packages/studio/src/tools/read-specs.ts` |
   | `packages/server/src/tools/save-answer-as-page.ts` | `packages/studio/src/tools/save-answer-as-page.ts` |
   | `packages/server/src/tools/answer-synthesis.ts` | `packages/studio/src/tools/answer-synthesis.ts` |
   | `packages/server/src/tools/update-index.ts` | `packages/studio/src/tools/update-index.ts` |
   | `packages/server/src/tools/lint-wiki.ts` | `packages/studio/src/tools/lint-wiki.ts` |
   | `packages/server/src/tools/get-schema-guidance.ts` | `packages/studio/src/tools/get-schema-guidance.ts` |
   | `packages/server/src/tools/preview-generation.ts` | `packages/studio/src/tools/preview-generation.ts` |
   | `packages/server/src/tools/generate-dependency-graph.ts` | `packages/studio/src/tools/generate-dependency-graph.ts` |
   | `packages/server/src/tools/build-graph.ts` | `packages/studio/src/tools/build-graph.ts` |
   | `packages/server/src/__tests__/<advanced-tool>.test.ts` (any present) | `packages/studio/src/__tests__/...` |
   | `packages/api/src/**` | `packages/studio/src/api/**` |
   | `packages/ui/src/**` | `packages/studio/src/ui/**` |
   | `packages/ui/index.html` | `packages/studio/index.html` (vite entry) |
   | `packages/ui/vite.config.ts` | `packages/studio/vite.config.ts` |

3. After the move, the 11 advanced tools' imports likely reference things
   like `../types`, `../filesystem`, `../language-map`, `../extractor`.
   Those modules are now in `@doquflow/core`. Update imports:
   ```ts
   // OLD (server-local)
   import { ModuleInfo } from "../types";
   import { walk, safeReadFile } from "../filesystem";

   // NEW (from core)
   import { types, filesystem } from "@doquflow/core/lib";
   ```
   (Match the actual export shapes from `@doquflow/core/lib`.)

4. Write `packages/studio/package.json`:
   ```json
   {
     "name": "@doquflow/studio",
     "version": "2.0.0",
     "description": "DocuFlow studio — advanced MCP tools, web UI, REST API, sync orchestration",
     "author": "Docuflow <hello@doquflows.dev>",
     "license": "MIT",
     "main": "./dist/lib.js",
     "types": "./dist/lib.d.ts",
     "bin": { "docuflow-studio": "./dist/mcp/index.js" },
     "files": ["dist/**", "ui-dist/**", "README.md"],
     "dependencies": {
       "@doquflow/core": "2.0.0",
       "@modelcontextprotocol/sdk": "^1.0.4",
       "cors": "^2.8.5",
       "express": "^4.19.2"
     },
     "devDependencies": {
       "@types/cors": "^2.8.17",
       "@types/express": "^4.17.21",
       "@types/node": "^22.0.0",
       "@types/react": "^18.3.0",
       "@types/react-dom": "^18.3.0",
       "@vitejs/plugin-react": "^4.3.0",
       "react": "^18.3.1",
       "react-dom": "^18.3.1",
       "typescript": "^5.6.0",
       "vite": "^5.4.0",
       "vitest": "^4.1.6"
     },
     "scripts": {
       "build": "tsc && vite build",
       "build:mcp": "tsc",
       "build:ui": "vite build",
       "dev:ui": "vite",
       "start:api": "node dist/api/index.js",
       "test": "vitest run"
     }
   }
   ```

5. Write `packages/studio/src/mcp/index.ts` — MCP server bootstrap:
   ```ts
   import { Server } from "@modelcontextprotocol/sdk/server/index.js";
   import { StdioServerTransport } from "@modelcontextprotocol/sdk/server/stdio.js";

   // 4 core tools — import the registration helpers from core (use the
   // existing core MCP server's tool definition as a model)
   import { coreToolDefinitions, handleCoreTool } from "@doquflow/core/lib";

   // 11 advanced tools — local
   import { listModules } from "../tools/list-modules";
   // ... (the other 10)

   // Register all 15
   // ... (use packages/server/src/index.ts as a template for the structure)
   ```
   The exact wiring depends on how `@doquflow/core` chose to expose its tool
   definitions. Coordinate with the @doquflow/core surface — if core exports
   a `toolDefinitions` array and a `handleTool` function, studio composes
   them. If core exports individual handler functions, studio assembles the
   full registry.

6. The OLD `packages/server/src/index.ts` (which #22 modified to import 4
   core tools from @doquflow/core) now has 11 LOCAL tool files that just got
   moved away. Update `packages/server/src/index.ts` to import all 11
   advanced tools from `@doquflow/studio/lib` (or similar) instead — this
   keeps server registering all 15 tools temporarily. **Final shim form
   happens in #24.**

   Equivalent: packages/server/src/index.ts becomes:
   ```ts
   import { coreToolDefinitions, handleCoreTool } from "@doquflow/core/lib";
   import { advancedToolDefinitions, handleAdvancedTool } from "@doquflow/studio/lib";
   // ... register all 15, dispatch to the right handler
   ```

7. Move test files following the same pattern. Each advanced tool's tests
   move with the tool.

8. Update `packages/ui/package.json` and `packages/api/package.json`:
   - Mark them as `"private": true` (they may already be)
   - You can leave them as empty placeholder workspaces, OR delete them
     entirely and update root `package.json` to remove them from the
     workspaces glob if needed.
   - Recommended: DELETE `packages/ui/` and `packages/api/` directories
     entirely after their contents are moved to `packages/studio/src/ui/`
     and `packages/studio/src/api/`. Update root tsconfig references if
     they list these paths.

## Acceptance criteria

- [ ] `npm install` succeeds at root
- [ ] `npm run build -w packages/studio` succeeds (both tsc and vite)
- [ ] `node packages/studio/dist/mcp/index.js` starts an MCP server registering EXACTLY 15 tools
- [ ] Smoke: `echo '{"jsonrpc":"2.0","id":1,"method":"tools/list"}' | node packages/studio/dist/mcp/index.js` returns 15 tools in `result.tools`
- [ ] UI dev server works: `npm run dev:ui -w packages/studio`
- [ ] UI production build works: `npm run build:ui -w packages/studio` and produces `packages/studio/dist/`
- [ ] REST API starts: `npm run start:api -w packages/studio`
- [ ] `grep -r "packages/server" packages/studio/src` returns NOTHING (studio is independent of server)
- [ ] `grep -r "../../server\|packages/server" packages/studio` returns NOTHING (no relative imports back to server)
- [ ] All advanced tool tests pass under `packages/studio/src/__tests__/`
- [ ] `packages/server` still builds and still registers 15 tools (now imported from core + studio)
- [ ] `bash scripts/pre-release-check.sh` PASSED
- [ ] PR body documents the file-move map

## Boundary rules
- `@doquflow/studio` depends ONLY on `@doquflow/core` and external libs (express, cors, react, vite, MCP SDK)
- `@doquflow/studio` does NOT depend on `@doquflow/server`
- The 4 core tools are imported from `@doquflow/core` — no duplication in studio

## Out of scope
- @doquflow/server becoming a shim (#24)
- CLI dep switch (#25)
- doctor command (#26)
- Migration docs (#27)

Closes #23.
```

---

# P2-a — Issue #24: `@doquflow/server` thin alias

```
Issue: https://github.com/shaifulshabuj/docuflow-mcp/issues/24
Branch: v2.0/issue-24-server-shim
[Paste universal preamble above]

## DEPENDENCY
Depends on #22 AND #23 being on main. Verify:
    git fetch origin main
    git log --oneline origin/main | grep -E "(#22|#23)"
Both must be present. If either is missing, STOP and report.

## Goal
`@doquflow/server` becomes a 1-file back-compat shim. Existing `.mcp.json`
configs registering `docuflow-server` keep working without any user action.
The shim depends on `@doquflow/studio` (which transitively pulls
`@doquflow/core`), so installing `@doquflow/server` v2.0.0 still produces a
working MCP server with all 15 tools.

This is the bright-line moment for v2.0: existing users SEE NOTHING DIFFERENT.

## Implementation steps

1. Delete `packages/server/src/` entirely. Replace with one file:

   `packages/server/src/index.ts`:
   ```ts
   #!/usr/bin/env node
   // @doquflow/server v2.0 is a back-compat alias of @doquflow/studio.
   // New installs should use @doquflow/studio (full surface) or
   // @doquflow/core (4-tool minimal). See release/v2.0.0.md.
   //
   // This entry re-uses studio's MCP server binary.
   import "@doquflow/studio/dist/mcp/index.js";
   ```

   (The import-with-side-effect pattern runs studio's MCP entry as if it
   were our own. Verify by checking how `packages/studio/src/mcp/index.ts`
   bootstraps — if it calls `runServer()` at module load, this works
   directly. If it exports a function, then this file becomes:
   ```ts
   #!/usr/bin/env node
   import { runServer } from "@doquflow/studio/dist/mcp/index.js";
   runServer();
   ```
   Pick whichever matches studio's actual shape.)

2. Update `packages/server/package.json`:
   ```json
   {
     "name": "@doquflow/server",
     "version": "2.0.0",
     "description": "DEPRECATED ALIAS: this package now re-exports @doquflow/studio. New installs should use @doquflow/studio or @doquflow/core. See https://github.com/doquflows/docuflow/blob/main/MIGRATION.md.",
     "author": "Docuflow <hello@doquflows.dev>",
     "license": "MIT",
     "homepage": "https://github.com/doquflows/docuflow",
     "repository": {
       "type": "git",
       "url": "https://github.com/doquflows/docuflow.git"
     },
     "bin": { "docuflow-server": "./dist/index.js" },
     "files": ["dist/**", "README.md"],
     "dependencies": {
       "@doquflow/studio": "2.0.0"
     },
     "devDependencies": {
       "@types/node": "^22.0.0",
       "typescript": "^5.6.0"
     },
     "scripts": {
       "build": "tsc"
     }
   }
   ```

   Note the removal of:
   - `@modelcontextprotocol/sdk` (transitive via studio)
   - Old keywords like "claude-code", "documentation" — keep them; they aid discoverability
   - The `"description"` field MUST clearly state "DEPRECATED ALIAS"

3. Rewrite `packages/server/README.md`:
   ```markdown
   # @doquflow/server (DEPRECATED ALIAS)

   This package was the original DocuFlow MCP server distribution.
   In v2.0+ it is a thin alias that depends on `@doquflow/studio`.

   ## Why does this still exist?

   Existing users have `@doquflow/server` registered in their `.mcp.json`
   files. The DocuFlow project's commitment is **zero breakage** for
   existing installs. This package will continue to install and run
   identically through all v2.x releases.

   ## What you should install instead

   - **Full surface** (UI, REST API, daemons, 15 MCP tools):
     `npm i -g @doquflow/studio`
   - **Irreducible value pipe** (4 MCP tools, no daemon, no UI):
     `npm i -g @doquflow/core`
   - **All-in-one CLI** (transitively pulls core + studio):
     `npm i -g @doquflow/cli`

   ## Migration

   See [MIGRATION.md](../../MIGRATION.md) and [release/v2.0.0.md](../../release/v2.0.0.md).

   Run `docuflow doctor` (v2.0+) for a personalized recommendation.

   ## Deprecation timeline

   - v2.0 — alias maintained, no behavior change
   - v2.x — alias maintained, no behavior change
   - v3.0 — removal *considered* based on download stats. We will publish
     a clear deprecation notice at least one minor version before any
     removal.
   ```

4. Update `packages/server/tsconfig.json` to compile just the single file:
   ```json
   {
     "compilerOptions": {
       "target": "ES2020",
       "module": "commonjs",
       "moduleResolution": "node",
       "esModuleInterop": true,
       "skipLibCheck": true,
       "strict": true,
       "outDir": "./dist",
       "rootDir": "./src",
       "declaration": true
     },
     "include": ["src/**/*"],
     "exclude": ["dist/**/*"]
   }
   ```

5. Delete any leftover files in `packages/server/`:
   - Old `src/tools/`, `src/types.ts`, etc. — should already be gone after #22/#23 moves, but verify
   - `src/__tests__/` if not already empty
   - Old `vitest.config.ts` if it was server-local (tests moved to core/studio)

## Acceptance criteria

- [ ] `npm i -g $(pwd)/packages/server` (or `npm install` then check) installs server
- [ ] `du -sh packages/server/dist` is < 5 KB — pure shim, no logic
- [ ] `docuflow-server` binary starts the same MCP server with 15 tools registered
- [ ] Smoke: `echo '{"jsonrpc":"2.0","id":1,"method":"tools/list"}' | docuflow-server` returns 15 tools
- [ ] `grep -rE "^(import|export)" packages/server/src` shows ONLY the single import of `@doquflow/studio` — no original implementation remaining
- [ ] No source files left under `packages/server/src/` other than `index.ts`
- [ ] README clearly marks DEPRECATED ALIAS
- [ ] `bash scripts/pre-release-check.sh` PASSED
- [ ] Pre-release-check script updated if needed (it may have had server-specific assertions that no longer apply)

## Out of scope
- CLI dependency switch (#25)
- doctor command (#26)
- Migration docs (#27)
- npm publish (release time)

Closes #24.
```

---

# P2-b — Issue #25: `@doquflow/cli` meta-package

```
Issue: https://github.com/shaifulshabuj/docuflow-mcp/issues/25
Branch: v2.0/issue-25-cli-meta-package
[Paste universal preamble above]

## DEPENDENCY
Depends on #22 AND #23 being on main. Can land in parallel with #24
(no file overlap between them). Verify:
    git fetch origin main
    git log --oneline origin/main | grep -E "(#22|#23)"
Both must be present. If either is missing, STOP and report.

## Goal
`@doquflow/cli` becomes a meta-package. It still provides the `docuflow`
binary, still implements `docuflow init / status / query / ingest / rewiki`
locally, but routes the rest to `@doquflow/studio`. It no longer depends
on `@doquflow/server`. Its source code shrinks; its behavior is unchanged.

`npm i -g @doquflow/cli@2.0.0` produces an install identical (from the
user's perspective) to v1.7.

## Required reading
Use the Explore sub-agent with this prompt:
    "Survey packages/cli/src/. For each command file under commands/,
    report which @doquflow/server tool(s) it invokes (look for the
    `loadServerTool` pattern). Identify the 5 core commands (init, status,
    query, ingest, rewiki) and the 9 advanced commands (watch, sync, ui,
    start, review, recent, suggest, update, watch-stop). For each command,
    note which @doquflow/core or @doquflow/studio export it should call
    after the meta-package switch. Under 400 words."

## Implementation steps

1. Update `packages/cli/package.json`:
   ```json
   {
     "name": "@doquflow/cli",
     "version": "2.0.0",
     "description": "DocuFlow CLI — meta-package providing the full surface (core + studio)",
     "author": "Docuflow <hello@doquflows.dev>",
     "license": "MIT",
     "homepage": "https://github.com/doquflows/docuflow",
     "repository": {
       "type": "git",
       "url": "https://github.com/doquflows/docuflow.git"
     },
     "bin": { "docuflow": "./dist/index.js" },
     "files": ["dist/**", "ui-dist/**", "README.md"],
     "dependencies": {
       "@doquflow/core": "2.0.0",
       "@doquflow/studio": "2.0.0",
       "cors": "^2.8.5",
       "express": "^4.19.2"
     }
   }
   ```
   - REMOVED: `@doquflow/server` (no longer needed; cli imports directly from core and studio)

2. Replace `loadServerTool()` calls with direct library imports.

   `packages/cli/src/commands/rewiki.ts`:
   ```ts
   // OLD: uses loadServerTool() shell-out
   // NEW:
   import { ingestSource, ... } from "@doquflow/core/lib";
   ```

   `packages/cli/src/commands/query.ts`:
   ```ts
   import { queryWiki, saveAnswerAsPage } from "@doquflow/core/lib";
   // (saveAnswerAsPage lives in studio actually — adjust the import based on
   // the Explore survey of where each function lives post-split)
   ```

   `packages/cli/src/commands/ingest.ts`:
   ```ts
   import { ingestSource } from "@doquflow/core/lib";
   ```

   For the 9 advanced commands (`watch`, `sync`, `ui`, `start`, `review`,
   `recent`, `suggest`, `update`, `watch-stop`):
   - Their existing TypeScript source in `packages/cli/src/commands/` stays
     where it is (cli-local logic — they're CLI commands, not MCP tools)
   - But if any of them call MCP tools (e.g. `sync` calls `lint_wiki`, or
     `ui` boots the studio REST API), they should import those from
     `@doquflow/studio/lib` instead of via `loadServerTool`

3. Verify the help text in `packages/cli/src/commands/help.ts` still
   reflects the same Core/Advanced grouping established in v1.7. No
   changes needed unless the package split shifted a command's home.

4. Verify the CLAUDE.md generator in `packages/cli/src/commands/init.ts`
   still produces the v1.7 4-core / 11-advanced layout. Adjust the tool
   names if the package split changed any (it shouldn't have — the MCP
   tool surface is identical, only the package boundaries moved).

5. Smoke-test every command after the dep switch:
   - `docuflow init` in a fresh tmp dir
   - `docuflow status` in this repo
   - `docuflow ingest <some-source>` (use a tmp project to avoid disturbing this repo's wiki)
   - `docuflow query "test"` against an ingested wiki
   - `docuflow rewiki --dry-run`
   - `docuflow watch status` (bare form, bright-line check)
   - `docuflow advanced watch status` (advanced form)
   - `docuflow sync --no-lint --quiet`
   - `docuflow review --staged` (in a tmp git repo)
   - `docuflow ui --help` (don't fully launch — just verify the command resolves)
   - `docuflow recent`
   - `docuflow suggest`
   - `docuflow update --check`

## Acceptance criteria

- [ ] `npm install` succeeds at root
- [ ] `npm run build -w packages/cli` succeeds
- [ ] `npm view` semantics: when installed via `npm i -g $(pwd)/packages/cli`, the resulting `package.json` lists `@doquflow/core` AND `@doquflow/studio`, does NOT list `@doquflow/server`
- [ ] Every existing CLI command works (smoke-tested per the list above)
- [ ] CI smoke tests (added in v1.7) pass unchanged — both bare-form and `advanced`-prefixed forms work
- [ ] Pre-release-check guard added: cli's package.json does NOT depend on `@doquflow/server` (greppable)
- [ ] No `loadServerTool` calls remain in cli's source
- [ ] `bash scripts/pre-release-check.sh` PASSED
- [ ] PR body documents the dependency-graph change and any function signatures that shifted between core and studio

## Boundary rules
- CLI's source is mostly routing — minimal business logic
- No `loadServerTool` shell-out anywhere (replaced with library imports)
- CLI's package.json does NOT list `@doquflow/server` as a dep

## Out of scope
- doctor command (#26)
- Migration docs (#27)

Closes #25.
```

---

# P3-a — Issue #26: `docuflow doctor` diagnostic command

```
Issue: https://github.com/shaifulshabuj/docuflow-mcp/issues/26
Branch: v2.0/issue-26-doctor-command
[Paste universal preamble above]

## DEPENDENCY
Depends on #25 being on main (need the new install paths to detect).
Can land in parallel with #27. Verify:
    git fetch origin main
    git log --oneline origin/main | grep -E "#25"
If not present, STOP and report.

## NOTE: this is the ONE new CLI subcommand the moratorium permits in v2.0.
It is a diagnostic that helps users navigate the package split, NOT a
feature that gives the system another thing to do. Re-read the moratorium
in the universal preamble before starting.

## Goal
Add `docuflow doctor` — a diagnostic that reports installed DocuFlow
packages, registered MCP servers, detected workflow, and actionable
recommendations. Helps users decide whether to switch from `@doquflow/cli`
to a smaller install path (or vice versa).

## Required reading
Use the Explore sub-agent with this prompt:
    "Survey three things:
     (1) How to detect globally-installed npm packages from Node.js. Read
         the npm docs or look for prior art in packages/cli/src/commands/
         (update.ts, status.ts may have similar logic). Identify the cross-
         platform shell-out pattern: `npm root -g`, `npm list -g --depth=0 --json`.
     (2) Where DocuFlow's CLI commands print colored output. Identify the
         color helper module — likely in packages/cli/src/commands/<some>.ts
         as inline ANSI helpers, or a shared module.
     (3) The structure of @doquflow/core's exports — what functions does it
         expose that doctor can call for the 'health summary' section?
         Specifically: is there a way to get page counts by category without
         running a full lint? Under 300 words."

## Command behavior

```
docuflow doctor                  # human-readable report
docuflow doctor --json           # machine-readable
docuflow doctor --quiet          # only print recommendations, no headers
```

Output sections (human-readable):

### Section 1 — Installed DocuFlow packages

Run `npm list -g --depth=0 --json` and parse. Report which of
`@doquflow/cli`, `@doquflow/core`, `@doquflow/studio`, `@doquflow/server`
are present, with their versions. Flag version mismatches (e.g. cli at 2.0
but server at 1.7).

### Section 2 — MCP server registration

Walk up from cwd looking for `.mcp.json` (stop at git root or home dir).
Parse each found file. For each `mcpServers` entry, check if it references
a DocuFlow binary (`docuflow-server`, `docuflow-studio`, `docuflow-core`,
or paths matching `**/docuflow*/dist/`). Report which binary is registered
and from which file.

Also check `~/.config/Claude/claude_desktop_config.json` if accessible
(macOS: `~/Library/Application Support/Claude/claude_desktop_config.json`).

### Section 3 — Workflow detection

Check `.docuflow/` contents in cwd:
- `.docuflow/watch.pid` exists → daemons in use
- `.docuflow/wiki/syntheses/query_*.md` count > 0 → query workflow active
- `.docuflow/specs/` has files → spec writing in use
- `.docuflow/wiki/entities/` count → just any wiki use at all

### Section 4 — Recommendations

Compute from the above:
- If only query/ingest workflow detected AND no daemon → recommend `@doquflow/core` as a smaller install
- If watch/sync/ui usage detected → recommend keeping `@doquflow/studio` or `@doquflow/cli`
- If `@doquflow/server` is registered but `@doquflow/studio` is also installed → recommend updating `.mcp.json` to use `docuflow-studio` directly (faster startup, no shim layer)
- If no `.docuflow/` found → recommend `docuflow init`

### Section 5 — Health summary

Pull a 3-line stat from `@doquflow/core`:
- Wiki page counts by category (entities/concepts/syntheses/timelines)
- Last update timestamp (from `.docuflow/log.md` tail)
- Don't run `lint_wiki` here — that's its own command

## Implementation

1. New file: `packages/core/src/commands/doctor.ts`
   - Pure logic; no I/O dependencies that aren't already in core
   - Export a `run(opts)` function that returns a structured report

2. Wire into `packages/cli/src/index.ts`:
   ```ts
   } else if (c === 'doctor') {
     import('@doquflow/core/lib').then(m =>
       m.runDoctor({
         json:  hasFlagIn(r, '--json'),
         quiet: hasFlagIn(r, '--quiet'),
       })
     );
   }
   ```

3. Add to `packages/cli/src/commands/help.ts` CORE section:
   ```
   docuflow doctor                       Diagnose your DocuFlow install
   ```

4. Update CLAUDE.md generation in `packages/cli/src/commands/init.ts` —
   mention `docuflow doctor` in the workflows section.

5. Smoke test in CI (`.github/workflows/ci.yml`):
   ```yaml
   - name: docuflow doctor produces parseable output
     run: |
       node packages/cli/dist/index.js doctor --json > /tmp/doctor.json
       jq '.installed_packages, .mcp_registration, .workflow' /tmp/doctor.json > /dev/null
       echo "  ✓ doctor --json output is valid JSON with expected sections"
   ```

## Acceptance criteria

- [ ] `docuflow doctor` produces a readable, accurate report on this repo
- [ ] All 5 sections render with real data
- [ ] `--json` output is valid JSON parseable by `jq`
- [ ] `--quiet` suppresses headers, only prints recommendations
- [ ] Handles missing `.docuflow/` gracefully (recommends `docuflow init`)
- [ ] Handles missing `.mcp.json` gracefully (note: agent registration unknown)
- [ ] Cross-platform: works on macOS and Linux (Windows tested via CI if matrix supports it; otherwise document the limitation)
- [ ] Smoke test added to CI; CI passes
- [ ] Help output lists `docuflow doctor` under CORE
- [ ] `bash scripts/pre-release-check.sh` PASSED

## Out of scope
- Auto-fixing (doctor only diagnoses)
- Wiki content health (that's `lint_wiki`)
- Cross-machine recommendations

Closes #26.
```

---

# P3-b — Issue #27: MIGRATION.md update + release/v2.0.0.md

```
Issue: https://github.com/shaifulshabuj/docuflow-mcp/issues/27
Branch: v2.0/issue-27-migration-and-release-notes
[Paste universal preamble above]

## DEPENDENCY (soft)
Best landed after #22, #23, #24, #25, #26 are all on main — so the document
can reference real PR numbers, accurate `du -sh` measurements, and real
`docuflow doctor` output. Verify:
    git fetch origin main
    git log --oneline origin/main | grep -E "(#22|#23|#24|#25|#26)"
All five should be present. You can draft in parallel with the last
issue's PR, just be ready to update numbers/snippets right before merge.

## Goal
Document the v2.0 package split for existing users. Headline: NOTHING IN
YOUR WORKFLOW HAS TO CHANGE. Provide options, a decision tree, a deprecation
timeline, and the empirical install-footprint numbers.

This is a documentation task. No code changes (beyond a pre-release-check
guard for the release notes file).

## Required reading (no sub-agent needed)

Read these to lift tone, structure, and exact numbers:
- `release/v1.7.0.md` — same milestone format
- `release/v1.6.0.md` — earlier milestone
- `MIGRATION.md` (current state — landed in v1.7)
- The 5 v2.0 PR bodies (#22–#26) for accurate technical detail
- The umbrella issue #1 (for the moratorium list and decision tree pieces)
- The LinkedIn post quoted in v1.6/v1.7 release notes:
  https://www.linkedin.com/posts/shaifulshabuj_softwareengineering-productdevelopment-softwarearchitecture-ugcPost-7461730048954474496-ASZF

## Implementation steps

### 1. Update `MIGRATION.md`

Add a new section after the v1.7 content:

```markdown
## What changes in v2.0 (package split)

DocuFlow splits into three packages. **Every existing install path keeps
working** — this section is about your *options*, not requirements.

### Install matrix

| Package | What you get | When to install |
|---|---|---|
| `@doquflow/cli` | Everything (transitively pulls core + studio) | Default. The path you're on today. |
| `@doquflow/studio` | 15 MCP tools + UI + REST + daemons + audit commands | You don't need the CLI wrapper, but you use the dashboard / watch / sync |
| `@doquflow/core` | 4 MCP tools + extractor + extractor rules | Minimal install. AI agent calls the tools directly. No daemon, no UI. |
| `@doquflow/server` | Back-compat alias of `@doquflow/studio` | DEPRECATED. Maintained through v2.x for existing `.mcp.json` configs. |

### Decision tree

```
Do you use docuflow watch / sync / ui / review / recent / suggest / update?
├── Yes   → keep @doquflow/cli OR install @doquflow/studio
└── No
    │
    └── Do you write specs, save synthesis pages, run dependency graphs?
        ├── Yes  → @doquflow/studio is the minimum
        └── No   → @doquflow/core is enough (much smaller install)
```

### Diagnostic

Run `docuflow doctor` (new in v2.0) for a personalized recommendation:

```
$ docuflow doctor
DocuFlow Doctor — installation diagnostic
─────────────────────────────────────────
Installed packages:
  @doquflow/cli       2.0.0
  @doquflow/core      2.0.0  (via cli)
  @doquflow/studio    2.0.0  (via cli)

MCP server registered:
  docuflow-server (legacy alias) → .mcp.json

Workflow detected:
  query usage: yes
  daemon usage: no
  spec writing: no

Recommendations:
  ✓ You're using query + ingest only. Switch to @doquflow/core:
      npm uninstall -g @doquflow/cli @doquflow/server
      npm i -g @doquflow/core
  ✓ Update .mcp.json: replace `docuflow-server` with `docuflow-core`
```

### Deprecation timeline

- v2.0 — `@doquflow/server` becomes a thin alias of `@doquflow/studio`. Installs and runs identically.
- v2.x — alias maintained, no behavior change
- v3.0 — removal *considered* based on download stats. A clear deprecation notice will be published at least one minor version before any removal.

### Reference table — every old command, where it lives now

| What you ran | Where it runs from in v2.0 |
|---|---|
| `docuflow init` | `@doquflow/cli` (local) |
| `docuflow status` | `@doquflow/cli` (local) |
| `docuflow query "..."` | `@doquflow/core` (lib import) |
| `docuflow ingest <file>` | `@doquflow/core` (lib import) |
| `docuflow rewiki` | `@doquflow/core` (lib import) |
| `docuflow doctor` | `@doquflow/core` (new in v2.0) |
| `docuflow watch` (& sub-commands) | `@doquflow/studio` |
| `docuflow sync` (& flags) | `@doquflow/studio` |
| `docuflow ui` / `docuflow start` | `@doquflow/studio` |
| `docuflow review` | `@doquflow/studio` |
| `docuflow recent` | `@doquflow/studio` |
| `docuflow suggest` | `@doquflow/studio` |
| `docuflow update` | `@doquflow/studio` |
```

### 2. Write `release/v2.0.0.md`

≤ 200 lines. Use `release/v1.7.0.md` as the format template. Sections:

```markdown
# DocuFlow v2.0.0 — Package Split

> "Every tool built to manage work eventually becomes work itself."
> Intent in → Value out → Nothing in between.

## Why this release exists

[1-2 paragraphs: v1.6 fixed quality, v1.7 relabeled the surface, v2.0
makes the core/advanced distinction physical. The split is the
precondition for evidence-based pruning in v3.0.]

## The bright line, held

Nothing in your workflow has to change. `npm i -g @doquflow/cli` still
gives you the full surface. `@doquflow/server` still installs and runs as
a thin alias. Every CLI command, every `.mcp.json` registration: unchanged.

## What landed

| PR | Issue | What it does |
|----|-------|--------------|
| #<n> | #22 | `@doquflow/core` package extracted (4 MCP tools + extractor) |
| #<n> | #23 | `@doquflow/studio` package extracted (11 advanced tools + UI + REST + daemons) |
| #<n> | #24 | `@doquflow/server` becomes a 1-file thin alias of studio |
| #<n> | #25 | `@doquflow/cli` becomes a meta-package depending on core + studio |
| #<n> | #26 | `docuflow doctor` diagnostic added |
| #<n> | #27 | MIGRATION.md + this file written |

(Fill in real PR numbers when you have them.)

## The new package architecture

```
@doquflow/cli  (meta-package — what users install)
   │
   ├─→ @doquflow/core   (4 tools: ingest_source, query_wiki, wiki_search, read_module)
   └─→ @doquflow/studio (11 advanced tools + UI + REST + daemons)
        │
        └─→ @doquflow/core

@doquflow/server (deprecated alias)
   └─→ @doquflow/studio
```

## Empirical install footprints

Run `du -sh` on the built `dist/` of each package and quote:

| Install path | Installed size |
|---|---:|
| `npm i -g @doquflow/cli` (full) | XX MB |
| `npm i -g @doquflow/studio` (no CLI wrapper) | XX MB |
| `npm i -g @doquflow/core` (minimal) | X MB |
| `npm i -g @doquflow/server` (alias) | < 5 KB |

(Measure for real before the release commit — these are placeholders.)

## docuflow doctor

[Show example output, demonstrating the new diagnostic.]

## What's next: v3.0

Evidence-based pruning. With v2.0 splitting the surface physically, we
can now measure usage of each package and each command honestly. If
`@doquflow/studio` features show real use a year after the split, they
earned their keep. If not, they retire — with notice.

## The "no" list, still in force

Every PR has to answer: *"This makes the user's problem easier, not our
system easier to build."*

- ❌ No new MCP tools (15 stays at 15)
- ❌ No new UI views
- ❌ No new integrations
- ⚠️ One new CLI subcommand in this release: `docuflow doctor` (diagnostic, not feature)
- ✅ Architecture clarity, install-footprint reduction, evidence-based future pruning

---

DocuFlow continues returning to its core. Thanks for the trust.
```

### 3. Update README.md

- Replace the current packages section with the new three-package
  architecture diagram (above)
- Link `MIGRATION.md` and `release/v2.0.0.md` prominently near the top
- Update the install instructions to show all three paths with one-liner
  descriptions

### 4. Update pre-release-check.sh

Add a guard:
```bash
test -f release/v2.0.0.md && check "release/v2.0.0.md exists" "pass" || check "release/v2.0.0.md exists" "fail"
```

## Acceptance criteria

- [ ] `MIGRATION.md` updated with the v2.0 section + install matrix + decision tree + deprecation timeline + reference table
- [ ] `release/v2.0.0.md` written (≤ 200 lines, mirrors release/v1.7.0.md format)
- [ ] README.md updated with three-package architecture + install paths
- [ ] All v2.0 PR numbers (#22 → real PR #s) referenced accurately, not as placeholders
- [ ] Empirical `du -sh` numbers for each install path measured on built dist (not placeholder text)
- [ ] Pre-release-check guard added for `release/v2.0.0.md` presence
- [ ] `bash scripts/pre-release-check.sh` PASSED

## Out of scope
- Cutting the actual release (version bump, tag, push) — that's the release manager step after this PR lands
- v3.0 detailed planning — vague-by-design until there's evidence

Closes #27.
```

---

## Parallelization plan

Four waves. Some pairs can run two agents simultaneously.

| Wave | Agent A | Agent B | Why parallel |
|------|---------|---------|--------------|
| 1 | #22 (extract core) | — | Foundation; everything else depends. Solo. |
| 2 | #23 (extract studio) | — | Depends on #22. Touches large surface; solo to avoid conflicts with parallel changes. |
| 3 | #24 (server shim) | #25 (cli meta) | Different packages (`packages/server/` vs `packages/cli/`), no file overlap |
| 4 | #26 (doctor) | #27 (docs) | Different files (`core/src/commands/doctor.ts` vs `MIGRATION.md` + `release/v2.0.0.md`) |

If only one agent is available, follow the priority order top-to-bottom.

## After v2.0

Once all six PRs land, run the release ritual mirroring the v1.7 pattern:

```bash
# 1. Demo the new install paths
npm pack -w packages/core   # confirm minimal package builds
npm pack -w packages/studio
npm pack -w packages/cli
npm pack -w packages/server

# 2. Run the bright-line verification
docuflow watch status                        # bare form still works
docuflow advanced watch status               # advanced form still works
docuflow doctor --json | jq .                # new diagnostic produces valid output
docuflow query "what changed in v2.0"        # core value pipe works end-to-end

# 3. Pre-release-check final
bash scripts/pre-release-check.sh            # must print RESULT: PASSED

# 4. Release ritual
#    - All 4 package.json files: bump version 2.0.0-alpha.x → 2.0.0 (if alphas were used)
#    - npm install --package-lock-only
#    - CHANGELOG.md + release/CHANGELOG.md: promote [Unreleased] → [2.0.0]
#    - Commit: "chore: release v2.0.0 — Package Split"
#    - Tag: v2.0.0 (annotated)
#    - Push main + tag
#    - gh release create v2.0.0 --notes-file release/v2.0.0.md
```

**Next milestone:** v3.0 — Evidence-based pruning. Defer issue filing
until v2.0 has been live for at least 3-6 months and we have honest data
on which `@doquflow/studio` features have real usage signal vs. which
sit dormant. The whole point of the v2.0 split is to enable that
measurement.
