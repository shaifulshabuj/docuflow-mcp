Got it — I’ll inspect the current git changes, review what should be improved, and then produce the spec in your exact template.

● skill(df-develop)

I’ve loaded the project skill context; next I’ll analyze the current git diff and derive a concrete implementation plan from the actual changes.

● Inspect working tree changes (shell)
  │ git --no-pager status --short && echo '---' && git --no-pager diff
  │ --name-status && echo '---' && git --no-pager diff --stat
  └ 40 lines...

● Show diffs for changed app code (shell)
  │ git --no-pager diff -- packages/cli/src/commands/ui.ts
  │ packages/ui/src/App.tsx packages/ui/src/components/Icon.tsx
  │ packages/ui/src/components/Rail.tsx packages/ui/src/components/TopBar.tsx
  │ packages/ui/src/context/ProjectContext.tsx
  │ packages/ui/src/styles/components.css …
  └ 15 lines...

● Read copilot-tool-output-1778264860577-3j6x1c.txt
  │ /var/folders/9d/gpld4vln75q5j_f0h29xswtm0000gn/T/copilot-tool-output-1778264
  │ 860577-3j6x1c.txt
  └ 1 line read

● Read copilot-tool-output-1778264860577-3j6x1c.txt
  │ /var/folders/9d/gpld4vln75q5j_f0h29xswtm0000gn/T/copilot-tool-output-1778264
  │ 860577-3j6x1c.txt
  └ L1:220 (220 lines read)

● Read copilot-tool-output-1778264860577-3j6x1c.txt
  │ /var/folders/9d/gpld4vln75q5j_f0h29xswtm0000gn/T/copilot-tool-output-1778264
  │ 860577-3j6x1c.txt
  └ L441:800 (319 lines read)

● Read copilot-tool-output-1778264860577-3j6x1c.txt
  │ /var/folders/9d/gpld4vln75q5j_f0h29xswtm0000gn/T/copilot-tool-output-1778264
  │ 860577-3j6x1c.txt
  └ L221:440 (220 lines read)

● Read App.tsx
  │ packages/ui/src/App.tsx
  └ 33 lines read

● Read TopBar.tsx
  │ packages/ui/src/components/TopBar.tsx
  └ 196 lines read

● Read ProjectContext.tsx
  │ packages/ui/src/context/ProjectContext.tsx
  └ 232 lines read

● Read SettingsView.tsx
  │ packages/ui/src/views/SettingsView.tsx
  └ 43 lines read

● Read Rail.tsx
  │ packages/ui/src/components/Rail.tsx
  └ 286 lines read

● Read ui.ts
  │ packages/cli/src/commands/ui.ts
  └ L200:460 (258 lines read)

● Read Icon.tsx
  │ packages/ui/src/components/Icon.tsx
  └ 73 lines read

I found several risky edits; I’ll run the current TypeScript/build checks to anchor the spec on concrete failures and fixes.

✗ Run UI/API typecheck and server/cli builds (shell)
  │ node_modules/.bin/tsc --noEmit -p packages/ui/tsconfig.json &&
  │ node_modules/.bin/tsc --noEmit -p packages/api/tsconfig.json && npm run
  │ build -w packages/server && npm run build -w packages/cli
  └ Permission denied and could not request permission from user

● Search (grep)
  │ "/api/sync|/api/init" (packages/api/src/index.ts,
  │ packages/cli/src/commands/ui.ts)
  └ 2 lines found

✗ Read types.ts
  │ packages/ui/src/types.ts
  └ Path does not exist

● Search (grep)
  │ "export type IconName|type IconName" (packages/ui/src)
  └ 1 line found

● Read index.ts
  │ packages/ui/src/types/index.ts
  └ 108 lines read

✗ Type-check UI package (shell)
  │ node_modules/.bin/tsc --noEmit -p packages/ui/tsconfig.json
  └ Permission denied and could not request permission from user

● Search (grep)
  │ "/api/sync|/api/init|settings" (packages/ui/src)
  └ 12 lines found

● Search (grep)
  │ "APP_VERSION|VITE_APP_VERSION|__APP_VERSION__" (packages/ui/src)
  └ 2 lines found

● Read index.ts
  │ packages/api/src/index.ts
  └ 304 lines read

● Search (grep)
  │ "export async function (syncWiki|initProject)" (packages/server/src/tools)
  └ No matches found

● Search (glob)
  │ "**/*sync*" in packages/server/src/tools
  └ No matches found

● Search (glob)
  │ "**/*.ts" in packages/server/src/tools
  └ 15 files found

● Search (glob)
  │ "**/*{sync,init}*.ts" in packages/cli/src/commands
  └ 3 files found

● Read init.ts
  │ packages/cli/src/commands/init.ts
  └ 1 line read

● Read sync.ts
  │ packages/cli/src/commands/sync.ts
  └ 392 lines read

# TASK-20260509-032707: Stabilize In-Flight UI/Server Changes

**Feature**: check the current git changes and review and improve  
**Type**: feature  
**Status**: ❌ rejected
**Created**: 2026-05-08T18:27:11Z

## Summary
Current uncommitted changes add new UI navigation/project-management behavior and new UI server endpoints, but the patchset introduces correctness risks (event-listener leaks, invalid React hook usage, stale state handling, and API/tool mismatches). This task hardens those changes without expanding scope, keeping behavior aligned between CLI-served UI and API dev bridge. The goal is a clean TypeScript/build state with predictable project selection/rescan/remove flows.

## Files to Touch
| File | Action | Reason |
|------|--------|--------|
| packages/ui/src/components/TopBar.tsx | MODIFY | Fix invalid `useState` ref usage, event-listener leak, context-menu lifecycle, and type safety |
| packages/ui/src/components/Rail.tsx | MODIFY | Remove unused locals/imports, stabilize picker behavior, and keep strict TS clean |
| packages/ui/src/context/ProjectContext.tsx | MODIFY | Fix rescan/remove logic, preserve selected project correctly, and avoid fabricated project data |
| packages/ui/src/App.tsx | MODIFY | Clean settings view render placement and keep view-switching readable/stable |
| packages/ui/src/views/SettingsView.tsx | MODIFY | Fix version fallback behavior and keep displayed values deterministic |
| packages/ui/src/components/Icon.tsx | MODIFY | Align icon exports/types used by Rail and avoid type drift |
| packages/ui/src/types/index.ts | MODIFY | Include any newly used icon names or remove unsupported icon branches |
| packages/cli/src/commands/ui.ts | MODIFY | Remove/replace broken tool loading (`sync-wiki`, `init-project`) and keep route surface valid |
| packages/api/src/index.ts | MODIFY | Mirror only supported routes and maintain dev/prod route parity |

## Implementation Steps

### Step 1: Fix TopBar interaction and lifecycle safety
**File**: `packages/ui/src/components/TopBar.tsx`
```ts
export default function TopBar(): JSX.Element
function openPicker(): void
async function handleAddPath(): Promise<void>
function handleRemove(path: string): void
function handleContextMenu(ev: MouseEvent, path: string): void
```
- Replace `const inputRef = useState<HTMLInputElement>(null)` with `useRef<HTMLInputElement | null>(null)` and remove `as any`.
- Move “close context menu on outside click” into `useEffect` with cleanup so listeners are not added on every render.
- Keep picker/context state transitions deterministic (open picker closes context menu, selecting/removing closes context menu).

### Step 2: Harden project state management
**File**: `packages/ui/src/context/ProjectContext.tsx`
```ts
export function ProjectProvider({ children }: { children: ReactNode }): JSX.Element
const addProjectByPath: (absPath: string) => Promise<AddResult>
function removeProject(absPath: string): void
async function rescanProjects(): Promise<void>
```
- Enforce “remove only custom paths” semantics (do not remove auto-discovered entries).
- In `rescanProjects`, do not synthesize entries with `{ ...res[0], path: cp }`; resolve custom paths via scoped API query and only append valid results.
- If removed path is currently selected, switch to next valid project or clear selection.
- Keep `isLoading` accurate for rescan lifecycle and avoid silent state corruption on partial failures.

### Step 3: Normalize rail/settings/icon typing and strict TS hygiene
**File**: `packages/ui/src/components/Rail.tsx`
```ts
interface RailProps { view: string; onChange: (v: string) => void }
export default function Rail({ view, onChange }: RailProps): JSX.Element
```
- Remove unused `React` import and unused locals (e.g., `name`) to satisfy strict UI compiler settings.
- Keep project picker close behavior consistent when clicking outside, selecting, and removing.
- Avoid direct mutable hover style hacks where class-based state already exists.

### Step 4: Fix Settings/App integration quality
**Files**: `packages/ui/src/App.tsx`, `packages/ui/src/views/SettingsView.tsx`
```ts
export default function App(): JSX.Element
export default function SettingsView(): JSX.Element
```
- Keep settings view conditional on its own line/section for maintainability.
- Replace string-literal version fallback (`'__APP_VERSION__' || 'dev'`) with a real runtime/build-safe fallback.

### Step 5: Resolve server route/tool mismatch
**Files**: `packages/cli/src/commands/ui.ts`, `packages/api/src/index.ts`
```ts
export async function run(opts: UiOptions = {}): Promise<void>
```
- Remove or replace unsupported tool loads (`sync-wiki`, `init-project`) that currently have no matching server tool module.
- Ensure `/api/*` surface is consistent between CLI `ui.ts` server and `packages/api/src/index.ts` (same supported routes, same request/response contract).
- If keeping `/api/sync` or `/api/init`, back them with real callable implementations; otherwise remove both route additions from both servers.

## Acceptance Criteria
- [ ] UI code has no strict TypeScript issues from the reviewed changes (unused locals, invalid hook typing, bad event handler types) — traceable to review issues #1, #2, #9, and #10.
- [ ] Project picker/remove/rescan behavior is deterministic and does not leak listeners — traceable to review issues #2, #3, #4, and #5.
- [ ] No server route references non-existent tool modules at runtime — traceable to review issue #6.
- [ ] CLI UI server and API dev bridge expose a consistent supported route set — traceable to review issue #7.
- [ ] Test evidence is written for every row in **Test Scenarios** with deterministic artifacts (scenario name, command/request, and artifact/output path), mapped 1:1 for review traceability — traceable to review issue #11.

## Edge Cases
- Case: User removes currently selected project → selection falls back to next available project or empty state without crash (traceable to review issue #5).
- Case: Rescan returns zero projects → UI keeps a valid empty state and does not fabricate project objects (traceable to review issue #4).
- Case: Right-click context menu opened repeatedly → only one active outside-click listener exists and is cleaned up (traceable to review issue #2).
- Case: Unsupported sync/init backend implementation → endpoint is absent (or returns explicit error), never a runtime module-load crash (traceable to review issues #6 and #7).
- Case: Settings version placeholder not injected → UI shows safe fallback value (`dev`) rather than raw placeholder token (traceable to review issue #8).
- Case: Test-scenario verification is requested by reviewers → each scenario has deterministic, artifact-based evidence attached in the same order as the Test Scenarios table (traceable to review issue #11).

## Test Scenarios
| Scenario | Input | Expected |
|----------|-------|----------|
| Happy path | Open UI, switch project, open Settings, trigger rescan | No runtime errors; project list/state updates correctly |
| Remove active project | Right-click active project → Remove | Active project switches safely; picker remains stable |
| Outside click cleanup | Open picker/context menu, click outside repeatedly | Menu closes correctly; no duplicated listener side effects |
| Rescan with missing custom path | Custom path no longer exists | Path is skipped/cleaned without crash or fake data |
| Route integrity | Start `docuflow ui` server | Startup succeeds; no missing-tool module load failure |
| Error | Call unsupported mutation endpoint (if intentionally removed) | Clear 404/explicit error response, not process crash |

## Copilot Instructions Block
```text
DEVLOOP TASK: TASK-20260509-032707
FEATURE: check the current git changes and review and improve
TYPE: feature

IMPLEMENT: stabilizeUiAndServerPatchset
SIGNATURE: export default function TopBar(): JSX.Element
SIGNATURE: export default function Rail({ view, onChange }: RailProps): JSX.Element
SIGNATURE: export function ProjectProvider({ children }: { children: ReactNode }): JSX.Element
SIGNATURE: export async function run(opts: UiOptions = {}): Promise<void>

RULES:
  1. Replace invalid hook/ref patterns and remove render-time event listener registration.
  2. Do not fabricate project entries during rescan; resolve paths through real API results only.
  3. Keep route parity between packages/cli/src/commands/ui.ts and packages/api/src/index.ts.
  4. Remove or properly back any endpoint that references missing server tool modules.
  5. Keep UI strict TypeScript clean (no unused locals/imports, no unsafe casts unless unavoidable and documented).

EDGE CASES:
  - active-project-remove: select next valid project or empty state without crash.
  - empty-rescan-result: stable empty UI, no fake object merge.
  - missing-sync-init-tool: explicit unsupported behavior, never startup crash.

TESTS REQUIRED: yes
FRAMEWORK: none
```


Changes   +0 -0
Requests  1 Premium (2m 49s)
Tokens    ↑ 1.3m • ↓ 8.6k • 1.3m (cached) • 4.5k (reasoning)
