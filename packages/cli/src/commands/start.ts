/**
 * docuflow start — alias for docuflow ui
 *
 * Starts the DocuFlow web interface (API bridge + Vite UI) on port 48821.
 * See ui.ts for full implementation.
 */

export { run, UiOptions } from './ui';
