# Docuflow Environment Configuration
# Copy this file to .env and fill in your values
# Never commit .env to git

# Server Configuration (optional overrides)
# DOCUFLOW_PORT is not currently used — Docuflow is stdio-only
# Reserved for future HTTP dashboard

# Development
NODE_ENV=development
