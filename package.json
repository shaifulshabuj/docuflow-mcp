{
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build -w packages/server && npm run build -w packages/ui && npm run build -w packages/cli && npm run build -w packages/api",
    "build:core": "npm run build -w packages/server && npm run build -w packages/cli",
    "build:ui": "npm run build -w packages/ui",
    "build:api": "npm run build -w packages/api",
    "release": "node scripts/release.js",
    "prerelease-check": "bash scripts/pre-release-check.sh",
    "start-web": "packages/ui/node_modules/.bin/vite packages/ui",
    "start-api": "packages/api/node_modules/.bin/tsx packages/api/src/index.ts",
    "dev:ui": "npm run dev -w packages/ui"
  }
}
